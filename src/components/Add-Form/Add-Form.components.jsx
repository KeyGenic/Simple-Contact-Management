import React,{useState} from 'react'
import './Add-Form.Styles.scss'

export const AddForm = () => {
    const [name,setName] = useState([]);
    const [email,emailName] = useState([]);
    const [number,numberName] = useState([]);

    return(
        <form id ="contact-form">
          
        </form>
    )
}